const START = 'start'
const PAUSE = 'pause'
const END = 'end'

const initialState = {

}

function reducer(state, action) {
  switch (action.type) {
    case START:
    case PAUSE:
    case END:


    default:
      break;
  }
}